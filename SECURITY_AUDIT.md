# 보안 감사 및 개선 계획

작성 기준: 2026-07-17, 커밋 `d58ab7b`

## 결론

현재 앱의 로그인 화면은 UI 접근만 제한하며 서버 데이터의 접근 권한을 보호하지 못합니다. 공개 웹 페이지를 아는 사람은 브라우저에 포함된 Supabase anon 키로 REST API를 직접 호출할 수 있고, 저장소의 초기 SQL대로라면 모든 테이블을 읽고 변경하거나 삭제할 수 있습니다. 실제 운영 데이터를 계속 사용하기 전에 Supabase Auth와 제한적인 RLS로 전환해야 합니다.

## 확인된 위험

### 긴급: 서버 권한이 사실상 공개됨

- `supabase_setup.sql`의 모든 정책이 `FOR ALL USING (true) WITH CHECK (true)`이다.
- 프런트엔드는 공개 anon 키로 모든 조회/추가/수정/삭제를 직접 수행한다.
- 앱의 자체 로그인 상태는 Supabase가 검증하지 않으므로 REST 요청을 막지 못한다.
- 영향: 회의록, 어르신 이름, 피드백, 처리 이력, 사용자 정보의 무단 열람·변조·삭제 가능성.

### 긴급: 비밀번호 평문 저장과 기본 관리자 계정

- `app_users.password`가 평문 `TEXT`이다.
- 앱이 전체 사용자 행을 브라우저로 내려받아 JavaScript로 비밀번호를 비교한다.
- 기본 `admin/1234`가 코드, SQL, 로컬 fallback에 존재한다.
- 영향: 사용자 비밀번호 노출, 관리자 사칭, 비밀번호 재사용 시 다른 계정 피해.

### 높음: 개인정보가 브라우저 저장소에 장기 보관됨

- 회의록, 어르신 명단, 사용자 목록, 처리 상태가 `localStorage`에 저장된다.
- 공유 PC의 다른 사용자나 같은 출처에서 실행되는 악성 스크립트가 접근할 수 있다.
- 로그아웃해도 데이터 캐시가 지워지지 않는다.

### 높음: Gemini API 키가 브라우저에 저장됨

- 키가 `localStorage`와 요청 URL 쿼리 문자열에 들어간다.
- XSS, 브라우저 확장, 공유 PC, 네트워크/진단 로그를 통해 노출될 수 있다.
- 사용량 도용과 비용 발생 위험이 있다.

### 높음: 저장형 XSS 가능성

- 사용자 입력과 DB 값을 조합한 여러 템플릿이 `innerHTML`에 들어간다.
- 일부 출력은 `esc()`를 사용하지만 모든 경로가 일관되게 이스케이프되지 않는다.
- 공격 문자열이 DB에 저장되면 다른 직원의 브라우저에서 실행될 수 있다.

### 중간: 레거시 운영본과 문서가 혼재

- `회의록_피드백관리.html`에는 다른 Supabase 프로젝트의 공개 키와 오래된 로직이 남아 있다.
- `PROJECT_CONTEXT.md`와 Git 기록에도 공개 키와 기본 비밀번호가 반복된다.
- anon 키는 원래 공개 가능한 값이지만, 프로젝트 혼동과 잘못된 배포 위험을 키운다.

## 권장 전환 순서

### 0. 즉시 조치

1. 채팅에 입력한 GitHub와 Supabase 계정 비밀번호를 각각 변경한다.
2. GitHub와 Supabase 계정에 MFA를 켜고 기존 세션을 로그아웃한다.
3. Supabase와 Gemini 사용량/감사 로그에서 알 수 없는 접근을 확인한다.
4. 운영 DB를 백업하고, 보안 전환이 끝날 때까지 앱 공유 범위를 최소화한다.
5. 기본 관리자 비밀번호를 더 이상 사용하지 않는다. 단, 이것만 바꿔서는 DB가 보호되지 않는다.

### 1. 인증 전환

1. Supabase Auth 이메일 로그인을 도입한다.
2. `app_users`의 `password` 컬럼을 제거하고 `auth.users.id`를 참조하는 `profiles` 테이블로 바꾼다.
3. 역할은 `profiles.role`에 두되 사용자가 자기 역할을 변경할 수 없도록 정책/관리 함수를 제한한다.
4. 프런트 요청의 `Authorization`에는 로그인 세션의 access token을 사용한다.
5. 비로그인 상태에서는 운영 데이터를 요청하지 않고 로컬 캐시도 표시하지 않는다.

### 2. RLS 전환

1. 기존 `allow_all_*` 정책을 모두 제거한다.
2. 최소한 `authenticated` 사용자만 업무 테이블을 읽고 쓰도록 한다.
3. 사용자 관리, 회의 삭제, 명단 전체 삭제 등 관리자 기능은 `profiles.role = 'admin'` 조건을 추가한다.
4. anon 역할에는 업무 테이블 권한을 주지 않는다.
5. SQL 적용 후 anon 키만 사용한 SELECT/INSERT/UPDATE/DELETE가 모두 거부되는지 확인한다.

### 3. 프런트엔드 보강

1. HTML 문자열 조립을 DOM 생성과 `textContent` 중심으로 교체한다.
2. 불가피한 HTML 렌더링은 검증된 sanitizer를 사용한다.
3. 모든 PostgREST 필터 값과 DOM 속성 값을 안전하게 인코딩한다.
4. 로그아웃 시 민감한 `localStorage` 값을 삭제한다.
5. 장기적으로 Gemini 호출을 인증된 서버/Edge Function으로 이동하고 사용자별 속도 제한을 둔다.
6. CSP, `Referrer-Policy`, `X-Content-Type-Options` 등 보안 헤더를 제공할 수 있는 호스팅으로 이전을 검토한다.

### 4. 검증과 배포

1. 별도 Supabase 테스트 프로젝트에서 스키마와 RLS를 검증한다.
2. 운영 DB 백업을 복원할 수 있는지 확인한다.
3. 관리자/일반 사용자/비로그인 사용자 권한 테스트를 자동화한다.
4. 모바일과 여러 기기에서 동기화·삭제·재로그인 회귀 테스트를 한다.
5. 운영 전환은 유지보수 시간을 정해 Auth 사용자 생성, DB 마이그레이션, 프런트 배포를 한 번에 수행한다.

## 완료 기준

- 비로그인/anon 요청으로 업무 테이블을 읽거나 바꿀 수 없다.
- DB와 브라우저 어디에도 사용자 평문 비밀번호가 없다.
- 기본 관리자 계정 또는 하드코딩된 사용자 비밀번호가 없다.
- 일반 사용자는 사용자/권한 관리 및 위험한 전체 삭제를 수행할 수 없다.
- 저장된 사용자 입력이 어느 화면에서도 스크립트로 실행되지 않는다.
- 로그아웃 후 공유 PC에 회의록과 명단 캐시가 남지 않는다.

## 주의

`supabase_setup.sql`을 운영 DB에 다시 실행하면 공개 정책이 재생성될 수 있다. 해당 파일은 역사적 참고용이며, Auth/RLS 마이그레이션이 준비되기 전에는 실행하지 않는다.
